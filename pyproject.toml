[tool.poetry]
name = "imap-oidc-bridge"
version = "0.1.0"
description = "Tiny OIDC identity provider that authenticates against an IMAP server. Lets any OIDC consumer (Authentik, Keycloak, Dex, etc.) trust mailbox passwords as the source of truth."
authors = ["Marc Schuetze <agent-provocateur@zkm.de>"]
license = "MIT"
readme = "README.md"
repository = "https://github.com/marc-schuetze/imap-oidc-bridge"
keywords = ["oidc", "imap", "authentik", "sso", "identity-provider"]
packages = [{ include = "imap_oidc_bridge" }]

[tool.poetry.dependencies]
python = "^3.12"
fastapi = "^0.115"
uvicorn = { extras = ["standard"], version = "^0.32" }
authlib = "^1.3"
cryptography = "^43.0"
pydantic = "^2.9"
pydantic-settings = "^2.6"
jinja2 = "^3.1"
python-multipart = "^0.0.20"
itsdangerous = "^2.2"
structlog = "^24.4"

[tool.poetry.group.dev.dependencies]
pytest = "^8.3"
pytest-asyncio = "^0.24"
httpx = "^0.27"
ruff = "^0.8"
mypy = "^1.13"

[tool.poetry.scripts]
imap-oidc-bridge = "imap_oidc_bridge.__main__:main"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "UP", "SIM", "RUF"]
ignore = ["E501"]

[tool.mypy]
python_version = "3.12"
strict = true
warn_unreachable = true

[[tool.mypy.overrides]]
module = ["authlib.*"]
ignore_missing_imports = true

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
